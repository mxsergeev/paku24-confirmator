require('dotenv').config()

const {
  PORT,
  AWS_ACCESS_KEY_ID,
  AWS_SECRET_ACCESS_KEY,
  PASSWORD,
  DOMAIN_NAME,
  MONGODB_URI,
  ACCESS_TOKEN_SECRET,
} = process.env

module.exports = {
  PORT,
  AWS_ACCESS_KEY_ID,
  AWS_SECRET_ACCESS_KEY,
  PASSWORD,
  DOMAIN_NAME,
  MONGODB_URI,
  ACCESS_TOKEN_SECRET,
}
